{
  "name": "feastly-backend",
  "type": "module",
  "imports": {
    "#*": "./*.js"
  },
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --watch --env-file=.env server.js",
    "test": "vitest --bail 1",
    "db:schema": "psql -d feastly -f db/schema.sql",
    "db:seed": "node --env-file=.env db/seed.js",
    "db:reset": "npm run db:schema && npm run db:seed"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.6",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.14.1"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.3.0",
    "install": "^0.13.0",
    "npm": "^11.11.1",
    "supertest": "^7.1.0",
    "vitest": "^3.1.2"
  },
  "engines": {
    "node": ">=22"
  }
}
